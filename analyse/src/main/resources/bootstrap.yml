server:
  port: 7003

spring:
  application:
    name: analyse
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      profile: dev

eureka:
  client:
    service-url:
      defaultZone: http://eureka:8888/eureka/