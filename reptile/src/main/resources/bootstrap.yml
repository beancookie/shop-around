server:
  port: 7002

spring:
  application:
    name: reptile
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      profile: dev

eureka:
  client:
    service-url:
      defaultZone: http://eureka:8888/eureka/
